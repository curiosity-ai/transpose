<Project Sdk="Transpose.Build.Target/26.9.4870">
    <PropertyGroup>
        <TargetFramework>netstandard2.1</TargetFramework>
        <!-- <LangVersion> is not set here on purpose: the Transpose.Build.Target SDK injects the
             one version the compiler supports, after this body, and `tps` ignores the project's
             own value. A pin would decide nothing and only mislead. -->
    </PropertyGroup>

    <ItemGroup>
        <PackageReference Include="Transpose.BCL" Version="26.9.4872" />
        <PackageReference Include="Transpose.Core" Version="26.9.4869" />
        <PackageReference Include="Transpose.Newtonsoft.Json" Version="26.9.4871" />
        <PackageReference Include="Tesserae" Version="2026.9.70484" />
    </ItemGroup>
</Project>
